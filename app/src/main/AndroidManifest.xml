<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthCareProject" >

<!--        <activity android:name=".jsonActivity" />-->

<!--        <activity android:name=".doctors" />-->

        <activity android:name=".patients" />

        <activity android:name=".dashboard" />

<!--        <activity android:name=".SimpleRecyclarViewExample" />-->

<!--&lt;!&ndash;        <activity android:name=".RecyclarDashboard" />&ndash;&gt;-->
<!--        <activity android:name=".BMI_activity" />-->
        <activity android:name=".ContentActivity" />
        <activity android:name=".ListViews" />

        <activity android:name=".login" />

        <activity android:name=".javaApp"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>